import 'package:flutter/material.dart';

Widget textdesign(data) {
  return Text(
        "$data",
        style: const TextStyle(
            color: Colors.black, fontSize: 20, fontWeight: FontWeight.w900,),
  );
}
